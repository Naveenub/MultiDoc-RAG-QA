version: "3.9"
services:
  backend:
    build: ./docker/Dockerfile.backend
    ports:
      - "8000:8000"
    env_file:
      - .env.example
  frontend:
    build: ./docker/Dockerfile.frontend
    ports:
      - "3000:3000"
